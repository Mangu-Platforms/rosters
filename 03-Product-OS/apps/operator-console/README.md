# Operator Console

Not built yet. The exception queue, cash position vs. next tax deposit, Health Score, and open compliance items — the surface the Operating Plan's Section 6 describes as "the person who used to live in ADP." No UI framework has been chosen yet. Build against `packages/schemas/exception.schema.json` and the `approval` table in `data/reference-data-model/schema.sql` once one is.
