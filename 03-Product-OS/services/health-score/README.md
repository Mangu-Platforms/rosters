# Health Score

Not built yet. The 0-100 workforce health metric (Operating Plan 5.5). Writes `health_score_snapshot` rows. Hold out a validation set and publish 90-day-exit hit rates before marketing this as predictive — if it can't beat a naive "missed two shifts" baseline, keep the components and drop the branding.
