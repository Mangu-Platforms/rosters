# Control Plane

Not built yet. The org / workplace / employee / pay-period / roles system of record — the first service to build, since every other service reads from it. Maps directly to Section 1 of `data/reference-data-model/schema.sql`.
