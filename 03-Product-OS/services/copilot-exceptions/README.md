# Copilot — Exception Detection

Not built yet. The real AI Payroll Copilot. `data/synthetic-crews/load_and_detect.py` is a rules-based reference baseline this service needs to beat, the same way the Health Score has to beat a naive baseline before it earns its branding (Operating Plan 5.5). Reads and writes `exception.schema.json` objects exclusively.
